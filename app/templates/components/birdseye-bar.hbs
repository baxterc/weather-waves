<div id="birdseye-bar">
  <div id="birdseye-bar-text">
    <h4>Current Conditions for {{weather.name}}</h4>
    <ul>
      <li>Temperature: {{to-fahrenheit weather.main.temp}}°F</li>
      <li>Conditions: {{fullConditions}}</li>
      <li>Wind: {{to-mph weather.wind.speed}} MPH from the {{wind-direction weather.wind.deg}} ({{weather.wind.deg}}°)</li>
      <li>Atmospheric Pressure: {{weather.main.pressure}} mbar</li>
      <li>Humidity: {{weather.main.humidity}}%</li>
    </ul>

    <button class="waves-effect waves-light btn" {{action 'generateWeather'}}>Animate Weather</button>
    <form>
      <div class="input-field">
        <label for="city">Enter a city (without spaces)</label>
        {{input value=city id="city"}}
      </div>
      <button class="waves-effect waves-light btn" {{action 'citySearch'}}>Get Weather</button>
      <button class="waves-effect waves-light btn" {{action 'weatherNow'}}>Show Model</button>
    </form>
  </div>
</div>
