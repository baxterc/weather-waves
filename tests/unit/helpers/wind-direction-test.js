import { windDirection } from 'weather-waves/helpers/wind-direction';
import { module, test } from 'qunit';

module('Unit | Helper | wind direction');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = windDirection([42]);
  assert.ok(result);
});
