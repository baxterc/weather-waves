import { toFahrenheit } from 'weather-waves/helpers/to-fahrenheit';
import { module, test } from 'qunit';

module('Unit | Helper | to fahrenheit');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toFahrenheit([42]);
  assert.ok(result);
});
