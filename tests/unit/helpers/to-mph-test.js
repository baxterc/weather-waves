import { toMph } from 'weather-waves/helpers/to-mph';
import { module, test } from 'qunit';

module('Unit | Helper | to mph');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = toMph([42]);
  assert.ok(result);
});
